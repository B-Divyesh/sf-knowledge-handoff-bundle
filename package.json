{
  "name": "knowledge-handoff-bundle-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build:site": "vite build",
    "build:demo": "sh scripts/build-demo.sh",
    "build": "cargo build --release --locked && npm run build:demo && npm run build:site",
    "test": "cargo test --locked && node --test tests/bundle-ui.test.mjs tests/site.test.mjs && npm run test:claims -- --test-name-pattern @claim:.*",
    "audit:a11y": "node scripts/a11y.mjs",
    "test:claims": "cargo build --locked && node scripts/run-claim.mjs",
    "package": "cargo package --locked --allow-dirty"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "playwright": "1.58.2",
    "vite": "^8.2.2"
  }
}
